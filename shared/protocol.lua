-- WiRe shared protocol module
-- Future home for packet names, discovery format and command helpers.
-- Do not move working server/client protocol here until we have mapped the old code safely.

local protocol = {}
protocol.version = "2.0.0-repo-start"
protocol.name = "WiRe"
return protocol
